// Check GETTING_STARTED.md for information regarding usage.
tasks.register("updatePortalExt", Copy) {
    from "${rootProject.projectDir}/configs/local/portal-ext.properties"
    into "${rootProject.projectDir}/bundles"
}